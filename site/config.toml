# Must match https://<user-or-org>.github.io/<repo> — update this when forking the template.
base_url = "https://edwbennett.github.io/rs-hello"

title = "Hello, Zola!"
description = "A minimal Zola site hosted on GitHub Pages."

compile_sass = false
build_search_index = false

[markdown]
highlight_code = true
